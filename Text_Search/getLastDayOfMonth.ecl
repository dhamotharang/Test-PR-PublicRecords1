IMPORT LIB_Date;

EXPORT UNSIGNED1 getLastDayOfMonth(UNSIGNED1 m, UNSIGNED2 y) := FUNCTION
	SET OF UNSIGNED1 DAYS := [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
	RETURN IF(m = 2 AND LIB_Date.LeapYear(y), DAYS[m] + 1, DAYS[m]);
END;