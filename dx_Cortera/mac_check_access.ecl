//*** Function created for CCPA suppressions at key fetches.
EXPORT mac_check_access(ds_in, ds_out, mod_access, append_contact) := MACRO
  // place holder for query changes on RoxieDev branch
  ds_out := ds_in;
ENDMACRO;
