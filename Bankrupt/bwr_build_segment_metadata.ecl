// Create from in-line segment list.

import bankrupt, text_search,BankruptcyV2;
// May need to change high level

//Changed the attribute to address bug# 157065
export bwr_build_segment_metadata(string filedate) := BankruptcyV2.BWR_Build_Segment_Metadata(filedate);