
// import doxie,business_header,DEA;

// export get_deaResults := MODULE
		// export search(dataset(assorted_layouts.Layout_search_IDs)in_deakeys, 
						// string in_ssn_mask_type) := function

				// dea_Owners_search :=DEAV2_Search_recs.search_owners(in_deakeys, in_ssn_mask_type);

				// return dea_Owners_search;
		// end;
// END;
export get_deaResults := 0;