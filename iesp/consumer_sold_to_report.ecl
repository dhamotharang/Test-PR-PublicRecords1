/*** Not to be hand edited (changes will be lost on re-generation) ***/
/*** ECL Interface generated by esdl2ecl version 1.0 from sold_to_report.xml. ***/
/*===================================================*/

import iesp;

export consumer_sold_to_report := MODULE
			
export t_SoldToReportOptions := record // iesp.share.t_BaseOption has a MakeGatewayCall option that is confusing and makes no sense here.
  boolean Blind {xpath('Blind')};//hidden[inhouse]
  string Encrypt {xpath('Encrypt')};//hidden[inhouse] ?? do we really need this?
end;
		
export t_SoldToReportBy := record
	iesp.share.t_Date RequestDate {xpath('RequestDate')};
	string30 LexId {xpath('LexId')};
end;	

export t_SoldToRecord := record
	string PrivacySourceID {xpath('PrivacySourceId')};
  string GlobalRecordID {xpath('GlobalRecordId')};
  utf8 Content {xpath('Content')};
end;
		
export t_SoldToSales := record
	iesp.share.t_Date SaleDate {xpath('SaleDate')};
	unsigned ClientGCID {xpath('ClientGCID')};
	dataset(iesp.share.t_StringArrayItem) SourceIds {xpath('SourceIds/SourceId'), MAXCOUNT(100)};
end;
		
export t_SoldToReportResult := record
  t_SoldToReportBy InputEcho {xpath('InputEcho')};
  string RoxieCluster {xpath('RoxieCluster')};
	dataset(t_SoldToSales) Sales {xpath('Sales/Sale'), MAXCOUNT(100)};
end;
		
export t_SoldToReportRequest := record (iesp.share.t_BaseRequest)
	t_SoldToReportOptions Options {xpath('Options')};
	t_SoldToReportBy ReportBy {xpath('ReportBy')};
end;

export t_SoldToReportResponse := record
  iesp.share.t_ResponseHeader _Header {xpath('Header')};
  t_SoldToReportResult Result {xpath('Result')};
end;
  
export t_SoldToReportResponseEx := record
	t_SoldToReportResponse response {xpath('response')};
end;

end;


/*** Not to be hand edited (changes will be lost on re-generation) ***/
/*** ECL Interface generated by esdl2ecl version 1.0 from sold_to_report.xml. ***/
/*===================================================*/

