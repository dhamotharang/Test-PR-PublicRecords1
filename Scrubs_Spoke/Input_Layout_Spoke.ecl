IMPORT Spoke;
//Using the keybuild layout because it is a fixed layout. Scrubs seemed to have an issue with the layout not being defined and tab delimited.
EXPORT Input_Layout_Spoke := Spoke.Layouts.Input.Keybuild;