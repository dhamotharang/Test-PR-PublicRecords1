//proc to build the lssi full file, use once only
import ut;

ut.mac_sf_buildprocess(lssi.bwr_lssi_build_file,'~thor_data400::base::lssi_main',build_full)

export proc_build_lssi_full_file := build_full;