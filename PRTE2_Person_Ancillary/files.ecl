import header,_control;
EXPORT files  := MODULE

//test cases: used for validation prcedures
EXPORT Ancillary_IN := DATASET(constants.test_cases, Layouts.Layout_in, CSV(HEADING(1), SEPARATOR('\t'), TERMINATOR(['\n','\r\n']), QUOTE('"')) );

EXPORT Ancillary_Temp := DATASET(constants.AncillaryTemp, layouts.Layout_Header, FLAT );
EXPORT Ancillary_TempB := DATASET(constants.AncillaryTempB, layouts.layout_header_v2_dl, FLAT );

Export ds := dataset(constants.SuperName1,layouts._main,thor);

export File_SSN_Map := dataset ( '~foreign::' + _control.IPAddress.prod_thor_dali + '::' +'thor_data400::base::ssnissue2', layouts.layout_ssn_map, flat);

export foreign_prod_boca := '~foreign::' + _control.IPAddress.prod_thor_dali + '::';

Export DS_Did :=    DATASET([ ],Layouts.arecord1);

Export DS_Dln :=    DATASET([ ],Layouts.arecord2);







































































































































END;