/*
Need to fix the keys--they don't run
also convert to version parameter
promotion/ rollback code to dataset based
automate with unix script, spray into build
find out problem with unix part that adds the url--the last part of the url is not being populated
also there is a bdid problem I think--they are all zeros
pkzipc problem, had to move it to edata14

*/