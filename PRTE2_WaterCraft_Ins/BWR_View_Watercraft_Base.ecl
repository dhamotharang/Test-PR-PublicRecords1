// PRTE2_WaterCraft_Ins.BWR_View_Watercraft_Base

IMPORT PRTE2_WaterCraft_Ins, PRTE2_WaterCraft_Ins.Utilities;

DS1 := PRTE2_WaterCraft_Ins.Datasets.All_Slim_Internal_Prod;
DS2 := PRTE2_WaterCraft_Ins.Datasets.All_Prod;
DS1;
DS2;

// DataIn := DS1;
// OUTPUT(COUNT(dataIn));

// report0 := RECORD
	// recTypeSrc	 := DataIn.st_registration;
	// GroupCount := COUNT(GROUP);
// END;

// RepTable0 := TABLE(DataIn,report0,st_registration);
// OUTPUT(SORT(RepTable0,recTypeSrc));

