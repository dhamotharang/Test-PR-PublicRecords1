import ut;

//OUTPUT(Domains.whois_did,,'~thor_data400::BASE::Whois_' + whois_build_date, overwrite);
ut.MAC_SF_BuildProcess(domains.whois_did,'~thor_data400::base::whois',do1,2);

export make_whois_base := do1;

