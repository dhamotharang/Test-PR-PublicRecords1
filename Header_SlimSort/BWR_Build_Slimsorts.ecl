import header;
#workunit('name','Slimsorts ' + header.version);
//Be sure to change version and file_headers in sandbox first
sequential(header_slimsort.Proc_Make_Name_xxx,
	header_slimsort.Proc_BuildKeys);