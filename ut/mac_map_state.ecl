import codes;
export mac_map_state(infile, infield, outfield, outfile) := macro
					 codes.mac_map_state(infile, infield, outfield, outfile);
endmacro : deprecated('Pleas use codes.mac_map_state instead of ut.mac_map_state'); 