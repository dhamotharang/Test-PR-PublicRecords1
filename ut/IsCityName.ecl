// Checks a string to see if it is in one of the seven sets that
// Set_CityNames had to be split into
import Address;
export BOOLEAN IsCityName(STRING city) := Address.IsCityName(city) : deprecated('Use Address.IsCityName instead.');