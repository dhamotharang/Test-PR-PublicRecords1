export Constants := Module
	// Experiment with 850 and 150 and a multiplier of 1,000 to replace the REAL4 with an UNSIGNED value.
	EXPORT Types.ScoreType alpha  							:= .80;
	EXPORT Types.ScoreType oneMinusAlpha				:= 1-alpha;
END;

