import doxie_build;

do := AllOffenses;

// export bwr_produce_OffenseFile := output(do,,'out::sex_offender_Offenses'+ doxie_build.buildstate + sexOffender.version,overwrite);
export OffenseFile := AllOffenses;