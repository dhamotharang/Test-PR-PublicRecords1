EXPORT Environment := MODULE

	EXPORT onThor_LeadIntegrity := false;  // this version of the shell eliminates the need to run certain portions of the shell because we don't use those in LeadIntegrity trending attributes
	EXPORT onThor := false;
	EXPORT onVault := false;
	
END;
