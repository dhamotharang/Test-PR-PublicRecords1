// Generally, suppressing records based on a source (for example, CCPA opt-out)
EXPORT mac_append_contact(ds_in, ds_out, mod_access, append_contact) := MACRO
  // place holder for query changes on RoxieDev branch
  ds_out := ds_in;
ENDMACRO;