EXPORT Constants := MODULE
	/* This turns on debugging while deploying the service.  Basically outputs every step of the process to help track problems. */
	EXPORT debugging := FALSE;
	// EXPORT debugging := TRUE;
END;